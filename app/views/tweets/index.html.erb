<div class="container">
  <div class="row">
  <div class="col-md-2  col-sm-10">
  </div>
  <div class="wrapper col-md-8  　col-sm-10">

  <p><%= notice %></p>


  <%= render 'form' %>
  
   <div id="list">
      <h1>ツイート一覧</h1>

      <%= @tweets.each do |tweet| %>
      <div class="row">
        <p><%= tweet.content %></p>
        <%= link_to "編集", edit_tweet_path(tweet.id), class: 'btn btn-default btn-sm edit'%>
        <%= link_to "削除", tweet_path(tweet), method: :delete, data: { confirm: '本当に削除していいですか？' },class: 'btn btn-default btn-sm btn delete' %>
      </div>
      <% end %>
   </div> 
  </div>
 
  </div>
</div>
<div id="gototop">
  <div id="gototop2"></div>
</div>

